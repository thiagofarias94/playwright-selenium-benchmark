# Playwright TypeScript Benchmark

This folder contains the Playwright benchmark implementation.

## Setup

```bash
cd playwright-ts
npm install
```

## Run benchmark

```bash
npm run benchmark
```

## Notes

- The benchmark is configured to repeat the same scenario 5 times.
- Compare the result time output with the Java Selenium benchmark.
