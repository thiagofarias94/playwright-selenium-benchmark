# Selenium Java Benchmark

This folder contains the Selenium benchmark implementation.

## Setup

```bash
cd selenium-java
mvn test-compile
```

## Run benchmark

```bash
mvn test
```

## Notes

- The sample test runs a headless Chrome browser with WebDriverManager.
- Use Maven output or `results/selenium-results.txt` to compare execution time.
